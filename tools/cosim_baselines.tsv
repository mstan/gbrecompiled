# Pinned co-simulation A-vs-B (recomp vs interpreter) baselines — the ratchet.
# The chain hash is the cumulative FNV fold of context-A's full-state hash at
# every T-cycle checkpoint. A recompiler/runtime change that alters guest
# behavior changes this hash; that is the regression signal.
#
# Regenerate/verify with (exe SHA-locks to its exact ROM; see rom.cfg gotcha):
#   python tools/gbc_cosim.py --exe <target>_test/build/<exe> \
#       --ab-frames <frames> --stride <stride> --expect-chain <chain>
#
# All rows below produced matched=1 (byte-identical full state at every
# checkpoint) on 2026-07-01, stride=456 (one scanline).
#
# 2026-07-02: re-pinned all three after the EI one-instruction-delay fix (gb_tick
# ime_pending counter; EI now sets ime_pending=2) and the CGB double-speed race
# window fix (race_win 2 vs 4). Both change guest-visible timing, so the full-
# state chains legitimately changed; A still == B (matched all checkpoints),
# blargg instr_timing still Passes, DMG oracle unchanged (2,266,364). The
# megaman_xtreme2 row is now measured (ROM staged at roms/megaman_xtreme2.gbc,
# sha1 cb1811ac..., gitignored) under CGB double-speed.
#
# target        stride  frames  chain
tetris          456     700     E92927C083145FD7
megaman_xtreme2 456     1000    B02E9D35794D298E
instr_timing    456     120     5D103AEB0D3F03DB
